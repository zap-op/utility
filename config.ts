export type TZapSpiderFullResultsConfig = {
    urlsInScope: number,
    urlsOutOfScope: number,
    urlsIoError: number
};

export type TZapAjaxFullResultsConfig = {
    inScope: number,
    outOfScope: number,
    errors: number
};
